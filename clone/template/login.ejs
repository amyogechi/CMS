<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login complaints System</title>
  <link rel="icon" type="image/png" href="assets/National-Open_University-Nigeria.png">
  <link rel="stylesheet" href="/index.css">
</head>

<body>
  <div class="main-wrapper">
    <!-- Left Image Section -->
    <div class="image-section">
      <img src="assets/National-Open_University-Nigeria.png" alt="NACOS Login" class="hero-image">
      <div class="image-overlay">
        <h2>Welcome to CMS</h2>
        <p>Your voice matters. We're here to listen and help resolve your concerns.</p>
      </div>
    </div>

    <!-- Right Form Section -->
    <div class="form-section">
      <div class="container">
        <header style="margin-bottom: 5px;">
          <span class="header">COMPLAINT MANAGEMENT SYSTEM</span>
        </header>
        <h4 style="margin-top: 5px;">Send in your complaints we are listening!</h4>
        <div class="form-container" style="margin-top: 10px;">
          <form id="loginForm" action="/login" method="post">
            <div class="label"><label for="login-username">Email</label></div>
            <div>
              <input type="email" id="login-username" name="email" placeholder="email" required>
            </div>
            <div class="label"><label for="password">Password</label></div>
            <div class="password-container">
              <input type="password" placeholder="password" name="password" id="password" required>
              <span onclick="togglePassword()" style="cursor:pointer;">👁️</span>
            </div>
            <!-- Forgot password link -->
            <div style="margin-top: 5px;">
              <a href="/changpassword" style="font-size: 0.9em; color: #1e90ff;">Forgot Password?</a>
            </div>
            <button class="sub-btn" type="submit">Login</button>
            <div>
              <p>Not registered? <a href="/registration">Register</a></p>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>

  <!-- <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
  <script src="api.js"></script>-->
  <script src="login.js"></script>

  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
  <!-- <script>
    const form = document.querySelector("form");

    form.addEventListener("submit", async (e) => {
      e.preventDefault();

      const formData = new FormData(form);
      const data = Object.fromEntries(formData.entries());

      try {
        const response = await fetch("/login", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify(data)
        });

        const result = await response.json();

        if (response.status === 200) {
          Swal.fire({
            icon: "success",
            title: "Login successful!",
            text: result.message,
            confirmButtonColor: "#3085d6",
          }).then(() => {
            // Redirect depending on role returned from backend
            window.location.href = "/admindash";
          });
        } else if (response.status === 201) {
          Swal.fire({
            icon: "success",
            title: "Login successful!",
            text: result.message,
            confirmButtonColor: "#3085d6",
          }).then(() => {
            window.location.href = "/complaintform";
            // localStorage.setItem("regno", result.regno); // optional
          });
        }
        else if (response.status === 401) {
          Swal.fire({
            icon: "error",
            title: "Password Error",
            text: result.message,
            confirmButtonColor: "#3085d6",
          });
        } else if (response.status === 404) {
          Swal.fire({
            icon: "error",
            title: "User Failed",
            text: result.message,
            confirmButtonColor: "#3085d6",
          });
        } else {
          Swal.fire({
            icon: "error",
            title: "Login Failed",
            text: result.message,
          });
        }
      } catch (error) {
        console.error("Error:", error);
        Swal.fire({
          icon: "error",
          title: "Oops...",
          text: "Something went wrong!",
        });
      }
    });
  </script> -->
  <script>
    const form = document.querySelector("form");

    form.addEventListener("submit", async (e) => {
      e.preventDefault();

      const formData = new FormData(form);
      const data = Object.fromEntries(formData.entries());

      try {
        const response = await fetch("/login", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify(data)
        });

        const result = await response.json();

        if (response.status === 200) {
          // Admin Login
          Swal.fire({
            icon: "success",
            title: "Login successful!",
            text: result.message
          }).then(() => {
            sessionStorage.setItem("email", result.email); // Save email
            window.location.href = "/admindash";
          });
        }

        else if (response.status === 201) {
          // Student Login
          Swal.fire({
            icon: "success",
            title: "Login successful!",
            text: result.message
          }).then(() => {
            sessionStorage.setItem("email", result.email); // Save email
            window.location.href = "/complaintform";
          });
        }

        else if (response.status === 401) {
          Swal.fire({
            icon: "error",
            title: "Password Error",
            text: result.message
          });
        }

        else if (response.status === 404) {
          Swal.fire({
            icon: "error",
            title: "User Failed",
            text: result.message
          });
        }

        else {
          Swal.fire({
            icon: "error",
            title: "Login Failed",
            text: result.message
          });
        }

      } catch (error) {
        console.error("Error:", error);
        Swal.fire({
          icon: "error",
          title: "Oops...",
          text: "Something went wrong!",
        });
      }

    });
    // result contains user info after successful login
    sessionStorage.setItem("email", result.email);

  </script>



</body>

</html>